/**
 * ============================================================================
 * AUTH FEATURE - PUBLIC API
 * ============================================================================
 *
 * Everything related to authentication UI (login, register, etc.)
 * Note: Auth service, guard, and interceptor are in core/auth
 */

export * from './components';
