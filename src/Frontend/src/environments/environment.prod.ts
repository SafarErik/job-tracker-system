export const environment = {
  production: true,
  apiBaseUrl: '', // Set via environment variable: VITE_API_BASE_URL or configure in Azure Static Web App settings
  googleClientId: '', // Set via environment variable: VITE_GOOGLE_CLIENT_ID or configure in Azure settings
};
